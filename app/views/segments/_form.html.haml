= simple_form_for(@segment) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, label: "名字"

  .controls
    = f.button :submit, "提交", class: "btn btn-primary"
    = link_to "取消", root_url, class: 'btn btn-info'
