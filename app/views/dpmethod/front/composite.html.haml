%p Composite pattern

%hr

%p= @message

= simple_form_for :test_form, html: { class: "form-horizontal" } do |f|
  = f.input :name, label: "名字", required: false
  = f.input :occup, label: "职称", required: false