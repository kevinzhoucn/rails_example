%h1 类别列表
.row
  .span5
    %table.table.table-striped
      %tr{style: "font-size:120%"}
        %th{style: "width:60%"} 名称
        %th{style: "width:40%"} 操作

      - @categories.each do |category|
        %tr
          %td
            = link_to category.name, category
          %td
            = link_to '修改', edit_category_path(category), class: 'btn btn-warning'
            = link_to '删除', category, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-danger'

%br

= link_to 'New Category', new_category_path
